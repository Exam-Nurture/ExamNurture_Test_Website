/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/ExamNurture_Test_Website',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
